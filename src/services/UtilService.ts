import { api, client, hasError } from '@/adapter';
import logger from '@/logger';
import store from '@/store';

const testFunction = async (query: any): Promise <any>  => {
  return 
}

export const UtilService = {
  testFunction
}